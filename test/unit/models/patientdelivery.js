// const assert = require('assert');
const helper = require('../../helper');
// const models = require('../../../models');

describe.skip('models.PatientDelivery', () => {
  beforeEach(async () => {
    await helper.loadFixtures(['patient']);
  });

  it('creates a new PatientDelivery record', async () => {
    // TODO
  });
});
