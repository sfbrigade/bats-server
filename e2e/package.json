{
  "name": "e2e",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "dependencies": {
    "@playwright/test": "^1.30.0",
    "dotenv": "^16.0.3"
  },
  "scripts": {
    "test": "DEBUG=pw:webserver playwright test",
    "install-chromium": "playwrigzht install --with-deps chromium"
  }
}
